
 var snowspeed = 0;
 var snowspeed1 = 4;

function snowSpawn1() {
   if (frameCount % 1 === 0) {
     var snow = createSprite(Math.round(random(0, width+100)), Math.round(random(-10, 0)));
     snow.addImage(snowImg);
     snow.scale = (random(0.001, 0.002));
     snow.lifetime = 130;
     snow.velocityX = snowspeed;
     snow.velocityY = snowspeed1+2;
   }
 }

 function snowSpawn2() {
   if (frameCount % 1 === 0) {
     var snow = createSprite(Math.round(random(0, width+100)), Math.round(random(-10, 0)));
     snow.addImage(snowImg);
     snow.scale = (random(0.003, 0.005));
     snow.lifetime = 130;
     snow.velocityX = snowspeed;
     snow.velocityY = snowspeed1+1;
   }
 }

 function snowSpawn3() {
   if (frameCount % 1 === 0) {
     var snow = createSprite(Math.round(random(0, width+100)), Math.round(random(-10, 0)));
     snow.addImage(snowImg);
     snow.scale = (random(0.004, 0.006));
     snow.lifetime = 130;
     snow.velocityX = snowspeed;
     snow.velocityY =snowspeed1+2;
   }
 }

 function snowSpawn4() {
   if (frameCount % 1 === 0) {
     var snow = createSprite(Math.round(random(0, width+100)), Math.round(random(-10, 0)));
     snow.addImage(snowImg);
     snow.scale = (random(0.008, 0.009));
     snow.lifetime = 130;
     snow.velocityX = snowspeed;
     snow.velocityY = snowspeed1+3;
   }
 }

 function snowSpawn5() {
   if (frameCount % 1 === 0) {
     var snow = createSprite(Math.round(random(0, width+100)), Math.round(random(-10, 0)));
     snow.addImage(snowImg);
     snow.scale = (random(0.002, 0.004));
     snow.lifetime = 130;
     snow.velocityX = snowspeed;
     snow.velocityY =snowspeed1+4;
   }
 }

 function snowSpawn6() {
   if (frameCount % 1 === 0) {
     var snow = createSprite(Math.round(random(0, width+100)), Math.round(random(-10, 0)));
     snow.addImage(snowImg);
     snow.scale = (random(0.004, 0.006));
     snow.lifetime = 130;
     snow.velocityX = snowspeed;
     snow.velocityY = snowspeed1+5;
   }
 }

 function snowSpawn7() {
   if (frameCount % 1 === 0) {
     var snow = createSprite(Math.round(random(0, width+100)), Math.round(random(-10, 0)));
     snow.addImage(snowImg);
     snow.scale = (random(0.004, 0.006));
     snow.lifetime = 130;
     snow.velocityX = snowspeed;
     snow.velocityY = snowspeed1+5;
   }
 }

 function snowSpawn8() {
   if (frameCount % 1 === 0) {
     var snow = createSprite(Math.round(random(0, width+100)), Math.round(random(-10, 0)));
     snow.addImage(snowImg);
     snow.scale = (random(0.004, 0.006));
     snow.lifetime = 130;
     snow.velocityX = snowspeed;
     snow.velocityY = snowspeed1+5;
   }
 }

 function snowSpawn9() {
   if (frameCount % 1 === 0) {
     var snow = createSprite(Math.round(random(0, width+100)), Math.round(random(-10, 0)));
     snow.addImage(snowImg);
     snow.scale = (random(0.004, 0.006));
     snow.lifetime = 130;
     snow.velocityX = snowspeed;
     snow.velocityY = snowspeed1+5;
   }
 }

 function snowSpawn10() {
   if (frameCount % 1 === 0) {
     var snow = createSprite(Math.round(random(0, width+100)), Math.round(random(-10, 0)));
     snow.addImage(snowImg);
     snow.scale = (random(0.004, 0.006));
     snow.lifetime = 130;
     snow.velocityX = snowspeed;
     snow.velocityY = snowspeed1+5;
   }
 }
